        y_tr, y_br = tr[1], br[1]
        delta_y = np.abs(y_tr - y_br)
        kc_y = delta_y / (N + 1)
        tmp_y = y_tr
        tmp_y_arr = []
        for _ in range(N):
            tmp_y+=int(kc_y)
            tmp_y_arr.append(tmp_y)
        
        tmp_x_arr = []
        for y in tmp_y_arr:
            tmp_x = int((y - b_tr_br)/a_tr_br)
            tmp_x_arr.append(tmp_x)

        lst_point_in_line_tr_br= list(zip(tmp_y_arr, tmp_x_arr))